name: Deploy Provider Microservice to MonsterASP

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'Provider.Services.API/**'
      - 'Welco.Shared/**'
      - '.github/workflows/deploy-provider.yml'
      - '.github/workflows/ftp-deploy.py'
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build & Deploy Provider Service
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Publish Provider Microservice
        run: |
          dotnet publish "Provider.Services.API/Provider.Services.API.csproj" \
            -c Release \
            -o ./publish/provider

      - name: Deploy to MonsterASP via FTP
        env:
          FTP_SERVER: ${{ secrets.PROVIDER_FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.PROVIDER_FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.PROVIDER_FTP_PASSWORD }}
          LOCAL_DIR: ./publish/provider
          REMOTE_DIR: /wwwroot
        run: python .github/workflows/ftp-deploy.py
