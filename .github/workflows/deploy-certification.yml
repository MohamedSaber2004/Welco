name: Deploy Certification Microservice to MonsterASP

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'Certification.Services.API/**'
      - 'Welco.Shared/**'
      - '.github/workflows/deploy-certification.yml'
      - '.github/workflows/ftp-deploy.py'
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build & Deploy Certification Service
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Publish Certification Microservice
        run: |
          dotnet publish "Certification.Services.API/Certification.Services.API.csproj" \
            -c Release \
            -o ./publish/certification

      - name: Deploy to MonsterASP via FTP
        env:
          FTP_SERVER: ${{ secrets.CERTIFICATION_FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.CERTIFICATION_FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.CERTIFICATION_FTP_PASSWORD }}
          LOCAL_DIR: ./publish/certification
          REMOTE_DIR: /wwwroot
        run: python .github/workflows/ftp-deploy.py
